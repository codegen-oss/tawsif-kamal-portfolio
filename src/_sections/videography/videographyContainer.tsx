import React from "react";
import { Profile } from "@/lib/types";

interface VideographyContainerProps {
  profile: Profile;
}

const VideographyContainer = ({ profile }: VideographyContainerProps) => {

};

export default VideographyContainer;
